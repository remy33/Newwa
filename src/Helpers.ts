
/**
 * Check if certain file is an audio file
 * @param filePath file to check
 */
export function isAudio(filePath:string) : boolean {
    return true;
}

/**
 * 
 * @param Id Checks if client is one of ours and should return trusted data
 */
export function isFriend(Id:any) {
    
}